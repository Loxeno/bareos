version: 2.1

executors:
  my-executor:
    machine: true
jobs:
  build: &build
    executor: my-executor
    steps:
      - checkout
      - run:
          name: Docker build Bareos
          command: |
            export TAG="${CIRCLE_BRANCH}-${BASE_IMG}"
            export BAREOS_PATH="${BAREOS_COMP}"
            echo 'export TAG="${CIRCLE_BRANCH}-${BASE_IMG}"' >> $BASH_ENV
            echo 'export BAREOS_PATH="${BAREOS_COMP}"' >> $BASH_ENV

            if [[ -n "$BAREOS_BACKEND" ]]
            then 
              export TAG="${CIRCLE_BRANCH}-${BASE_IMG}-${BAREOS_BACKEND}"
              export BAREOS_PATH="${BAREOS_COMP}-${BAREOS_BACKEND}"
              echo 'export TAG="${CIRCLE_BRANCH}-${BASE_IMG}-${BAREOS_BACKEND}"' >> $BASH_ENV
              echo 'export BAREOS_PATH="${BAREOS_COMP}-${BAREOS_BACKEND}"' >> $BASH_ENV
            fi
            docker build -t barcus/bareos-${BAREOS_COMP}:${TAG} ${BAREOS_PATH}/$BASE_IMG
      - run:
          name: Save images
          command: |
            mkdir images
            echo "1 ${BAREOS_PATH} - 2 ${BAREOS_COMP} - 3 ${TAG}"
            docker save -o images/image-${BAREOS_PATH}.tar barcus/bareos-${BAREOS_COMP}:${TAG}
      - persist_to_workspace:
          root: .
          paths:
            - ./images

  deploy: &deploy
    executor: my-executor
    steps:
      - attach_workspace:
          at: /tmp/workspace
      - run:
          name: Load image and add tags
          command: |
            export TAG="${CIRCLE_BRANCH}-${BASE_IMG}"
            export BAREOS_PATH="${BAREOS_COMP}"
            echo 'export TAG="${CIRCLE_BRANCH}-${BASE_IMG}"' >> $BASH_ENV

            if [[ -n "$BAREOS_BACKEND" ]]
            then
              export TAG="${CIRCLE_BRANCH}-${BASE_IMG}-${BAREOS_BACKEND}"
              export BAREOS_PATH="${BAREOS_COMP}-${BAREOS_BACKEND}"
              echo 'export TAG="${CIRCLE_BRANCH}-${BASE_IMG}-${BAREOS_BACKEND}"' >> $BASH_ENV
            fi

            docker load -i /tmp/workspace/images/image-${BAREOS_PATH}.tar
            docker tag barcus/bareos-${BAREOS_COMP}:${TAG} barcus/bareos-${BAREOS_COMP}:${CIRCLE_BRANCH}
      - run: 
          name: Docker login
          command: docker login -u $DOCKER_USER -p $DOCKER_PASS
      - run:
          name: Docker push
          command: |
            docker push barcus/bareos-${BAREOS_COMP}:${CIRCLE_BRANCH}
            docker push barcus/bareos-${BAREOS_COMP}:${TAG}

  build_client_ubuntu:
     <<: *build
     environment:
       - BAREOS_COMP: client
       - BASE_IMG: ubuntu

  deploy_client_ubuntu:
     <<: *deploy
     environment:
       - BAREOS_COMP: client
       - BASE_IMG: ubuntu




workflows:
  version: 2
  build-n-deploy:
    jobs:
      - build_client_ubuntu
      - deploy_client_ubuntu:
          requires:
            - build_client_ubuntu
      #- build:
      #    name: build director-ubuntu-mysql
      #    context: director-ubuntu-mysql
      #- build:
      #    name: build director-ubuntu-pgsql
      #    context: director-ubuntu-pgsql
      #- build:
      #    name: build client-ubuntu
      #    context: client-ubuntu
      #- build:
      #    name: build storage-ubuntu
      #    context: storage-ubuntu
      #- build:
      #    name: build webui-ubuntu
      #    context: webui-ubuntu
      #- deploy:
      #    name: deploy director-buntu-mysql
      #    context: director-ubuntu-mysql
      #    requires:
      #      - build director-ubuntu-mysql
      #- deploy:
      #    name: deploy director-ubuntu-pgsql
      #    context: director-ubuntu-pgsql
      #    requires:
      #      - build director-ubuntu-pgsql
      #- deploy:
      #    name: deploy client-ubuntu
      #    context: client-ubuntu
      #    requires:
      #      - build client-ubuntu
      #- deploy:
      #    name: deploy storage-buntu
      #    context: storage-ubuntu
      #    requires:
      #      - build storage-ubuntu
      #- deploy:
      #    name: deploy webui-ubuntu
      #    context: webui-ubuntu
      #    requires:
      #      - build webui-ubuntu
